export type SeoRouteDefinition = {
  descriptionKey: string;
  path: string;
  robots: string;
  titleKey: string;
};

export type SeoMetadata = {
  description: string;
  robots: string;
  title: string;
};
