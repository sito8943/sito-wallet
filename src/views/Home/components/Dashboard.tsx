// cards
import { TransactionTypeResume } from "./Cards";

export const Dashboard = () => {
  return (
    <section id="dashboard" className="flex items-center justify-center w-full">
      <TransactionTypeResume />
    </section>
  );
};
